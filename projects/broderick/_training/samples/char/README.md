# Char LoRA (brdmc) — validated samples

Rendered 2026-06-14 with char LoRA @1.0 + ink style block + avatar description + brdmc.
cs_phone/point/shrug = ONE consistent avatar (bald, bearded, glasses) in B&W ink across 3 poses.
NOTE: bare "brdmc, <pose>" does NOT work on Flux — needs the style block + identity described.
