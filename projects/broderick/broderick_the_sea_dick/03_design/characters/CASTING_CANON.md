# Casting canon (from style_kernel.json — do not contradict)


Drop each character's locked hero portrait in `anchors/<character>.png` — the keyframe path will --cref against it for cross-shot face consistency.
