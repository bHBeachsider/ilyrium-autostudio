// Tailwind CSS v4 uses a dedicated PostCSS plugin (@tailwindcss/postcss).
// No autoprefixer needed — v4 handles vendor prefixing itself.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
